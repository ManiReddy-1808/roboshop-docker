[uwsgi]
wsgi-file = payment.py
callable = app

master = true
processes = 1
lazy-apps = true
enable-threads = true

socket = 0.0.0.0:8080
protocol = http

; Bcz alpine uid starts with 101, so no need to create user and group.
; uid = 1001
; gid = 1001

